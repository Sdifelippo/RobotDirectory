<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  <title>Robot Directory</title>
  <link rel="stylesheet" href="robot.css"/>
</head>

<body>
  <h1 class="header">Robots Looking for Work</h1>
  <div class = "needwrapper">
    {{ #staff }}
    <div class="upper">
      <div><img src="{{ avatar }}" class="needphoto"></div>
      <div class="info">
        <span><h1>{{ name }}</h1></span>
        <span><p>{{ #address.city }}{{ address.city }},{{ /address.city }} {{ address.country }} | {{ #job }} {{ job }}, {{ /job }} {{ ^job }} <span class= "available"> Available for hire</span>{{ /job }} {{ company }}</p></span>
        <span><p>{{ email }} | {{ phone }}</p></span>
      </div>
    </div>
  <div class="bottomwrapper">
    <div class="category">
      <span>
      <h4>Education</h4>
      <h4>Skills</h4>
    </span>
    </div>
    <div class="attributes">
      <p>{{ university }}</p>
      {{ #skills }}
      <ul>
        <li>{{ . }}</li>
      </ul>
      {{ /skills }}
    </div>
  </div>
    {{ /staff }}

  </div>

</body>

</html>
