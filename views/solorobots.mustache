<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
    <link rel="stylesheet" href="/robot.css"/>
  <title>Robots</title>

</head>

<body>

  <h4>Index</h4>
  <!-- //this is the loop -->
  <div class="main">
    {{ #robotdata }}
    <div class="wrapper"
    <a href="http://localhost:3000/individual_Robot/{{name}}">
      <img class="Avatars" src="{{avatar}}" alt="{{name}}"></a>
    <h1 class="robotName">{{name}}</h1>
        {{ #job }}
        {{ job }},
        {{ /job }}
        {{ ^job }}
        <p class="available">Available for hire</p>
        {{ /job }}
    <p>{{company}}</p>
  </div>
    {{ /robotdata }}
  </div>
</body>



</html>
